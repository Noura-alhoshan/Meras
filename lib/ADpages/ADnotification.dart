import 'package:flutter/material.dart';

class ADnotification extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('صفحة التنبيهات'),
    );
  }
}
